name: anatomy_heatmap
description: Flutter anatomical SVG heatmaps with deterministic muscle mapping semantics.
version: 0.0.1
homepage: https://github.com/kdhfred/anatomy_heatmap
repository: https://github.com/kdhfred/anatomy_heatmap
issue_tracker: https://github.com/kdhfred/anatomy_heatmap/issues

environment:
  sdk: ^3.11.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  path_drawing: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
