name: anatomy_heatmap_example
description: Minimal example for the anatomy_heatmap package.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  anatomy_heatmap:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
